import { Col, Row, Card, Button } from "react-bootstrap"
import React from "react"
const Land = (props) => {

    return (
        <Col key = {props.id}>
            <Button key = {props.id}>
            </Button>
        </Col>
    )
}

export default Land