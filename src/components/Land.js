import { Col, Row, Card, Button } from "react-bootstrap"
import React from "react"
const Land = (props) => {

    return (
        <Col>
            <Button>
            </Button>
        </Col>
    )
}

export default Land