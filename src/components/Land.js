import { Col, Row } from "react-bootstrap"
import React from "react"
const Land = (props) => {

    return (
        <>
        <Row>id = {props.id} name = {props.name} </Row>
        </>

    )
}

export default Land